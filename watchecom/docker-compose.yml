version: "3.9"

services:
  frontend:
    container_name: watch-frontend
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "80:3000"
    networks:
      - frontend-network
    depends_on:
      api-gateway:
        condition: service_healthy
    restart: unless-stopped

  api-gateway:
    container_name: api-gateway
    build:
      context: ./backend/api-gateway
      dockerfile: Dockerfile
    environment:
      - PORT=8080
      - REGISTRY_URL=http://registry-service:8761
      - LOGGING_URL=http://logging-service:9000/logs
    ports:
      - "8080:8080"
    networks:
      - frontend-network
      - backend-network
    depends_on:
      registry-service:
        condition: service_healthy
      logging-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://localhost:8080/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  registry-service:
    container_name: registry-service
    build:
      context: ./backend/registry-service
      dockerfile: Dockerfile
    environment:
      - PORT=8761
      - REGISTRY_TTL_MS=90000
    ports:
      - "8761:8761"
    networks:
      - backend-network
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://localhost:8761/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  user-service:
    container_name: user-service
    build:
      context: ./backend/services/user-service
      dockerfile: Dockerfile
    environment:
      - SERVICE_NAME=user-service
      - PORT=8081
      - REGISTRY_URL=http://registry-service:8761
      - DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      - DB_SCHEMA=users
      - JWT_SECRET=${JWT_SECRET}
      - LOGGING_URL=http://logging-service:9000/logs
    ports:
      - "8081:8081"
    networks:
      - backend-network
    depends_on:
      registry-service:
        condition: service_healthy
      postgres:
        condition: service_healthy
      logging-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://localhost:8081/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  product-service:
    container_name: product-service
    build:
      context: ./backend/services/product-service
      dockerfile: Dockerfile
    environment:
      - SERVICE_NAME=product-service
      - PORT=8082
      - REGISTRY_URL=http://registry-service:8761
      - DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      - DB_SCHEMA=products
      - LOGGING_URL=http://logging-service:9000/logs
    ports:
      - "8082:8082"
    networks:
      - backend-network
    depends_on:
      registry-service:
        condition: service_healthy
      postgres:
        condition: service_healthy
      logging-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://localhost:8082/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  cart-service:
    container_name: cart-service
    build:
      context: ./backend/services/cart-service
      dockerfile: Dockerfile
    environment:
      - SERVICE_NAME=cart-service
      - PORT=8083
      - REGISTRY_URL=http://registry-service:8761
      - DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      - DB_SCHEMA=cart
      - LOGGING_URL=http://logging-service:9000/logs
    ports:
      - "8083:8083"
    networks:
      - backend-network
    depends_on:
      registry-service:
        condition: service_healthy
      postgres:
        condition: service_healthy
      logging-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://localhost:8083/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  order-service:
    container_name: order-service
    build:
      context: ./backend/services/order-service
      dockerfile: Dockerfile
    environment:
      - SERVICE_NAME=order-service
      - PORT=8084
      - REGISTRY_URL=http://registry-service:8761
      - DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      - DB_SCHEMA=orders
      - LOGGING_URL=http://logging-service:9000/logs
    ports:
      - "8084:8084"
    networks:
      - backend-network
    depends_on:
      registry-service:
        condition: service_healthy
      postgres:
        condition: service_healthy
      logging-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://localhost:8084/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  review-service:
    container_name: review-service
    build:
      context: ./backend/services/review-service
      dockerfile: Dockerfile
    environment:
      - SERVICE_NAME=review-service
      - PORT=8085
      - REGISTRY_URL=http://registry-service:8761
      - DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      - DB_SCHEMA=reviews
      - LOGGING_URL=http://logging-service:9000/logs
    ports:
      - "8085:8085"
    networks:
      - backend-network
    depends_on:
      registry-service:
        condition: service_healthy
      postgres:
        condition: service_healthy
      logging-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://localhost:8085/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  postgres:
    container_name: watch-postgres
    image: postgres:16-alpine
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./backend/db/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    ports:
      - "5432:5432"
    networks:
      - backend-network
      - database-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  logging-service:
    container_name: logging-service
    build:
      context: ./backend/logging-service
      dockerfile: Dockerfile
    environment:
      - PORT=9000
    ports:
      - "9000:9000"
    networks:
      - backend-network
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://localhost:9000/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  postgres-data:

networks:
  frontend-network:
    name: frontend-network
  backend-network:
    name: backend-network
  database-network:
    name: database-network
